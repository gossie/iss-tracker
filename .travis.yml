dist: trusty
sudo: required
language: node_js
node_js:
  - "node"
script:
  - npm run build
  - npm test
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $ISS_TRACKER_GITHUB_TOKEN
  keep-history: true
  local-dir: build
  on:
    branch: master
